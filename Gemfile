source 'https://rubygems.org'
ruby '2.1.1'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.2'
# Use SCSS for stylesheets
gem 'sass-rails', '~> 5.0'

gem 'uglifier', '>= 1.3.0'

# Use CoffeeScript for .js.coffee assets and views
gem 'coffee-rails', '~> 4.1.0'

# Use jquery as the JavaScript library
gem 'jquery-rails'

# Use jquery UI
gem 'jquery-ui-rails'

gem 'jbuilder', '~> 2.0'

# Angular.js library
gem 'angularjs-rails'

# underscore js library
gem 'underscore-rails'

# make forms easier
gem 'formtastic'
gem 'formtastic-bootstrap'

# bootstrap
gem 'bootstrap-sass'

# bootstrap datetimepicker
gem 'momentjs-rails', '>= 2.9.0'
gem 'bootstrap3-datetimepicker-rails', '~> 4.15.35'

# devise and cancan for auth
gem 'devise'
gem 'angular_rails_csrf'
gem 'cancan'
gem 'execjs'
gem 'therubyracer'
gem 'high_voltage'

# postgres
gem 'pg'

gem 'simple_form'

# slim templates are more better
gem 'slim-rails'

# Styling a SVG document with CSS for use on the web
gem 'inline_svg'

# factual api
gem 'factual-api'

# Acts As Votable for Rails/ActiveRecord models
gem 'acts_as_votable', '~> 0.10.0'

# paginate browse results
gem 'will_paginate'

# Underscore js
gem 'underscore-rails'

gem "httparty"
gem 'multi_xml'

# Geokit Rails
gem 'geokit-rails'

group :development do
  gem 'better_errors'
  gem 'binding_of_caller', :platforms=>[:mri_21]
  gem 'quiet_assets'
  gem 'rails_layout'
  gem 'spring-commands-rspec'
  gem 'letter_opener'

  # capistrano for deployment
  gem 'capistrano-rails'
  gem 'capistrano-rvm'
  gem 'capistrano-bundler'
  gem 'capistrano-passenger'
end

group :development, :test do
  gem 'byebug'
  gem 'web-console', '~> 2.0'
  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'spring'
  gem 'factory_girl_rails'
  gem 'faker'
  gem 'pry-rails'
  gem 'pry-rescue'
  gem 'pry-remote'
  gem 'pry-nav'
  gem 'rspec-rails'
end
# capistrano for development
group :test do
  gem 'capybara'
  gem 'database_cleaner'
  gem 'launchy'
  gem 'selenium-webdriver'
end
